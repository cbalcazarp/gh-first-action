name: Runner OS Version Check
description: This is a simple GitHub Actions workflow to build a Node.js project.
on: workflow_dispatch
jobs:
  runner-version:
    runs-on: ubuntu-latest
    steps:
      - name: Get runner version
        run: cat /etc/os-release